import { View, Text } from "react-native";

export default function QuestsScreen({}) {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 26, fontWeight: "bold" }}>Quests Screen</Text>
    </View>
  );
}
